/**
 * BullMQ worker entry — provisioning, billing, email, scheduled jobs.
 * No HTTP ports; talks to Redis, Postgres, and external APIs only.
 */
export function bootstrap(): void {
  // Wired in a later Phase 0 task.
}
