{
  "name": "@app/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo \"Next.js app scaffolding deferred to Phase 1 storefront slice\"",
    "build": "echo \"web build pending\"",
    "start": "echo \"web start pending\"",
    "lint": "echo \"lint pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@app/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
